# Copy to .env.local for local development without the Vite proxy
# In Vercel: set VITE_API_BASE_URL in Project Settings → Environment Variables

# Leave empty to use the Vite dev proxy (recommended for local dev)
VITE_API_BASE_URL=

# Production example:
# VITE_API_BASE_URL=https://expense-tracker-prod.railway.app
